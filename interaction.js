const btnMobile = document.getElementById('btn-mobile');
function toogleMenu(){
    const menuMobile = document.getElementsById('menuMobile');
    nav.classList.toogleMenu('active');

}
btnMobile.addEventListener('click', toogleMenu);